node_modules
dist

# Secrets — nunca commitar
.env
.env.*
!.env.example
.paytrackr-lan-sync.json
*.local
*.tsbuildinfo
.DS_Store

# PDFs / dados reais só para teste local (ex.: fatura Itaú de referência)
private/
