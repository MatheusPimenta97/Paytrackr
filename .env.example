# Copie para `.env` na raiz e preencha (veja README ou docs do assistente).
# Chave em https://platform.openai.com/api-keys — use projeto com billing.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
VITE_AI_ASSISTANT_URL=/api/paytrackr/assistant/image
